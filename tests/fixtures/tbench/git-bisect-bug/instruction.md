# Git Bisect Bug Fix Task

Fix the regression in `regression.js` so that `compute()` returns `42`.

Ensure `regression.js` is valid CommonJS:
```javascript
module.exports = {
  compute: () => 42,
};
```
